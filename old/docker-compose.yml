services:
  food-save-website:
    container_name: food-save-website
    image: food-save-website:latest
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    ports:
      - '127.0.0.1:8080:80'
